---
export type Props = {
	slug: string | undefined;
	shouldCount?: boolean;
}

const { slug, shouldCount = false } = Astro.props
---

<div class="inline" data-slug={slug} data-count={shouldCount ? '1' : '0' }>--- views</div>
